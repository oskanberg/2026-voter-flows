# Tested with Python 3.12. The pinned versions are what produced the
# figures and matrix checked into this repo; later versions of these
# packages should generally work too.
numpy==2.4.4
scipy==1.17.1
pymc==5.28.5
arviz==0.23.4
matplotlib==3.10.9
plotly==6.7.0
kaleido==1.3.0
